[package]
name = "charis-desktop"
version = "0.1.0"
edition = "2024"

[dependencies]
charis-core = { path = "../charis-core" }
tauri = { version = "2", features = [] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1.40", features = ["full"] }

[build-dependencies]
tauri-build = { version = "2", features = [] }

